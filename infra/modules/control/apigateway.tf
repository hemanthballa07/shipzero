# API Gateway — Phase 7
# HTTP API (not REST API) for cost optimization.
#
# Resources to create:
#   aws_apigatewayv2_api.main
#   aws_apigatewayv2_stage.default
#   aws_apigatewayv2_route (one per endpoint)
#   aws_apigatewayv2_integration (one per lambda)
