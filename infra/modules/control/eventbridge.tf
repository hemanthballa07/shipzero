# EventBridge — Phase 8
# Rules on default event bus for build lifecycle events.
#
# Resources to create:
#   aws_cloudwatch_event_rule (one per event type)
#   aws_cloudwatch_event_target (connect rules to targets)
