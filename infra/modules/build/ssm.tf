# SSM Parameter Store — Phase 6
# Standard tier, SecureString for secrets.
# Sensitive values created manually via CLI, referenced here with data sources.
#
# Parameters:
#   /shipzero/dev/github/webhook_secret   (SecureString, manual)
#   /shipzero/dev/github/app_private_key_pem (SecureString, manual)
#   /shipzero/dev/github/app_id           (String)
#   /shipzero/dev/github/installation_id  (String)
